#include "math.hpp"

#include <cmath>

namespace ne {

    Float sqrt(Float x){
        //TODO: For now
        return ::sqrt(x);
    }

    Float invSqrt(Float x){
        //TODO: For now
        return 1 / sqrt(x);
    }
}